<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="review.mapper">

	<select id="list" resultType="review.ReviewVO">
			select * from review where customer_email = #{customer_email, jdbcType=VARCHAR}
	</select>
	
	
	<!-- 	<select id="detail" resultType="store.StoreVO">
		select * from store where customer_email = #{customer_email, jdbcType=VARCHAR}
		</select>
	 -->
<!-- 	select s.* , st.*
		from store s left outer join state st on s.store_number = st.store_number
		where customer_email = #{customer_email, jdbcType=VARCHAR}
		ORDER BY st.inventory_number
	</select>  -->
	
	
	<!--now state 불러오는 쿼리  -->
<!-- <select id="detail" resultType="store.StoreVO">
		select s.*, (select now_state from state where s.store_number = store_number) now_state 
		from store s
		where customer_email = #{customer_email, jdbcType=VARCHAR}
	</select>  -->
	
	
<!-- 	<select id="detail" resultType="store.StoreVO"> -->
<!-- 		select * from store where store_number = #{store_number, jdbcType=VARCHAR} -->
<!-- 	</select> -->

</mapper>